<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <a asp-controller="Game" asp-action="Find" class="navbar-brand">Chess Platform</a>
            <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target="#navbar-main">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div id="navbar-main" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li><a asp-controller="Game" asp-action="New">Let's start!</a></li>
                <li><a asp-controller="Ranking" asp-action="Players">Leaderboard</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right unselectable">
                <li class="dropdown user-info">
                    <a id="user" class="dropdown-toggle" data-toggle="dropdown">
                        @UserService.GetUserWithProfileByUsername(User.Identity.Name).Profile.Nickname
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a asp-controller="User" asp-action="Profile" asp-route-id="@UserService.GetUserByUsername(User.Identity.Name).ExternalId">Profile</a></li>
                        <li class="divider"></li>
                        <li><a asp-controller="Auth" asp-action="Logout">Log out</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>